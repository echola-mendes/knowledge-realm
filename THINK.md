# THINK：导图抽题 / 回答评分（未成熟）

**状态：** 草稿想法。不是需求，不是 V1 承诺。成熟前不要写进 `design-document.md`，不要排进 `implementation-plan.md`，不要写代码。

**来源：** 提出人口述（面试 XMind 会忘；希望导入导图后抽题、AI 评分、定位回导图）。

**当前版本不做：** 不按问题跳到 XMind 客户端/节点位置。现网仍无 `.xmind` 导入代码。

想法上（仍非规格）：扩展名为 **`.xmind`** 的导入 **只切问题**（标题末尾「？」或 `?` 的节点），不把整张导图当普通文档全文切块。

---

## 1. 原始诉求（尽量原意）

- 面试题以 **XMind** 整理；看过会忘，想练。
- 希望：导入导图 → **随机出题** → 自己答 → **AI 评价**。导图里题目标题末尾带 **「？」或 `?`**。
- 评价要能 **快速定位** 到该题在导图里的位置。
- **不限面试**：所有知识库里的导图都应能用；以后也可能当「随机挑战」。
- 评分：**十分制，按回答里正确部分占比**（不是对/错两档）；指出缺漏与更好表述；太复杂时用 **Markdown / Mermaid 文本**。

格式偏好（口述，后补充）：不只 XMind，希望 **多种格式** 都能作为出题来源。

补充（口述）：

- 练习前可以 **选板块**，例子：JAVA 后端面试、AGENT 面试、日常学习。
- 板块应对上 **已经有数据的知识库**（跟现有库走，不是另起一套空壳）。
- **标签** 用来把文档/内容 **关联到上述模块**，再据此 **随机生成问题**。
- 抽题 **先不按遗忘曲线**。再加开关 **「复习错题」**：开则 **错误率越高越容易出现**；关则普通随机（不按错误率加权）。
- **「AI面试」开关** 同时管题库页和首页挑战：开则高错误率/低占比分的题更靠前。无对错二元。另有 **「复习错题」** 开关。
- **每道题打标签：正确率**；按正确率颜色：**红 &lt;40%、黄 40%–69%、绿 70%+**（40% 算黄）。
- 导入识别 **`.xmind`**：只切问题，不按整图全文切。
- 题库 **单独一张业务表**（不与 `document_chunk` 混用）。
- 问题和答案下方展示知识库文档里的 **关键部分**：**打开题时从 `document_chunk` 抽一段**，不落库摘录，文档改完再索引则展示跟着变。
- 首页提问框 **右侧** 为 **每日一题**：只显示一题，下方 **「开启挑战」**。点开后进入 **今日挑战详情**，详情里才有 **「复习错题」** 开关。
- 文档原题、衍生题、关联知识是 **三道题**，分别记正确率。
- 导图切题看标题末尾 **「？」或 `?`**，都算。
- 抽题跟 **顶栏当前知识库** 走。
- 评分里的图示用 **Markdown / Mermaid 文本**（不生图）。
- 遗忘八档（**先不用**，以后可再开）：5 分钟 → 30 分钟 → 12 小时 → 1 天 → 2 天 → 4 天 → 7 天 → 15 天。
- 单轮题数 **上限 50 道**。
- **参考链接由提出人自己填**。
- 每道题可写 **Markdown 笔记**。

---

## 1.1 提出人答问（仍非规格）

问的是澄清，不是拍板。编号按当时提问。

1. **题从哪来：** 根据已打标签的上传文档。例：JAVA基础、KAFKA 等标签关联的文档；后端面试模块可选这些标签。做该模块时题目来源于这些文档：可以用文档里的问题、也可以问该问题的衍生问题、也可以问相关联的知识。
2. **关系：** 板块（模块）可以关联标签；标签关联文档；知识库下有文档；**题库来源于模块下的文档**。
3. **随机挑战位置：** 首页右侧标题 **「每日一题」**。点「开启挑战」进 **今日挑战详情**；**「复习错题」只在详情页**。
4. **正确率：** 每次 AI 给 0–10，含义是 **这次回答正确部分占比**。总正确率仍是累计分/累计满分。例：4/10 + 7/10 → 11/20。
5. **继续挑战：** 默认 10 道；继续时自己选题数，**上限 50 道**。
6. **抽题：** 先不用遗忘曲线。开关 **「复习错题」**（**今日挑战详情页**）：开 = 错误率越高越容易抽到；关 = 均匀随机。
7. **题库看答案：** AI 评语里的正确答案；参考链接 **提出人手填**；也可以是导入材料里的原文。
8. **XMind 里何为题：** 问题标题末尾 **「？」或半角 `?`** 都算。
9. **范围冻结（当前已落地版本）：** 不做导图内定位（不跳 XMind 节点）。导入 `.xmind` / 只切问题 **尚未写进现网**。
10. **导入识别：** 路径/扩展名为 **`.xmind`** 则 **只切问题**。
11. **存储：** 题库 **单独一张业务表**，不塞进 `document_chunk`。列草案见下节。
12. **出处展示：** 问题和答案下展示知识库 **关键部分**；**打开时从 `document_chunk` 现取**，题表不存摘录正文。
13. **首页一题：** 默认 10 道的第一题；点「开启挑战」开始这一轮。
14. **原题 / 衍生 / 关联：** **三道题**，正确率分开记。
15. **问号：** 切题看末尾 **「？」或 `?`**，都算。
16. **文档节奏：** 需求讨论清楚后再进 `design-document.md`。
17. **抽题范围：** 跟 **当前选中知识库**。
18. **图示：** Markdown / Mermaid 文本，不生图。
19. **遗忘档：** 已列出八档但 **先不用于抽题**。
20. **参考链接：** 提出人 **手填**。
21. **题笔记：** 每道题可记 Markdown。
22. **面试再练：** 与加权随机一致：占比分低（错误率高）下次更容易出现。偏低阈值未定。
23. **「复习错题」开关：** 在 **开启挑战后的详情页**，不在首页每日一题卡上。

---

## 1.2 题库表字段草案（仅 THINK，不是规格）

表名暂定 `practice_question`。仍是 **一张业务表**：正确率用累计列维护，不另建作答流水表。**颜色标签落库** `color_tag`，每次更新 `score_sum`/`max_sum` 后按正确率改写。分类沿用文档已有标签（`document_tag`），本题表不重复存文档标签。模块不写在本题表上（模块勾选标签再圈文档）。**关键部分不进本题表**，打开时读 `document_chunk`。

| 字段 | 类型 | 说明 |
|---|---|---|
| id | UUID PK | |
| knowledge_base_id | UUID FK | 所在知识库 |
| document_id | UUID FK | 来源文档 |
| stem | TEXT | 题干 |
| origin | TEXT | `extracted` 文档/导图原题；`derived` 衍生；`related` 关联知识 |
| answer | TEXT 可空 | 参考答案（AI 评语或导入原文） |
| reference_url | TEXT 可空 | 参考链接，提出人手填 |
| note_md | TEXT 可空 | 该题的 Markdown 笔记 |
| source_heading | TEXT 可空 | 解析稿标题；打开题时用来在 `document_chunk` 里对上一段 |
| source_chunk_id | UUID 可空 FK | 可选，指向 `document_chunk`；reindex 后可能失效，失效则按 document_id + heading 再找 |
| xmind_topic_id | TEXT 可空 | 导图节点 id；**当前版本不用于跳转客户端** |
| score_sum | INT 默认 0 | 累计得分（如 4+7） |
| max_sum | INT 默认 0 | 累计满分（如 10+10） |
| color_tag | TEXT 可空 | 题目颜色：`red` / `yellow` / `green`；从未作答为空 |
| attempt_count | INT 默认 0 | 出现次数 |
| last_score | INT 可空 | 最近一次 0–10 |
| last_attempt_at | TIMESTAMPTZ 可空 | 最近作答 |
| next_review_at | TIMESTAMPTZ 可空 | 预留；**抽题先不用** |
| review_step | INT 默认 0 | 预留八档；**抽题先不用** |
| created_at | TIMESTAMPTZ | |
| updated_at | TIMESTAMPTZ | |

抽题（先）：只抽 **当前知识库**。关「复习错题」均匀随机；开则按错误率加权。遗忘八档 **暂不参与抽题**。

表里 **没有** 存 R = e^{-t/S} 那种公式参数。间隔档与正确率/颜色是两套。

正确率 = `score_sum / max_sum`（`max_sum=0` 表示还没答过）。写入 `color_tag`：**&lt;40% `red`；40%–69% `yellow`（含 40%）；≥70% `green`**。

建议唯一：同一 `document_id` + 同一 `stem` 不重复插入。文档删除时本题行 CASCADE。

未写入 `design-document.md`，未建迁移。

---

---

## 2. 尚未拍板（禁止当已定）

下面只是讨论过的方向，**全部可推翻**。

- 「关键部分」一次展示多长；reindex 后 `source_chunk_id` 怎么补。
- 从未作答的题在「复习错题」打开时怎么加权。
- 「AI面试」与「复习错题」同时开时如何叠加。

---

## 3. 和现有 V1 的张力（观察，不是方案）

当前已落地 / 已设计的 V1 是：导入常见文档、切块、余弦检索、RAG 对话、标签收藏。  
本想法会多出一类交互：**模块关联标签 → 标签圈文档 → 出题 → 作答评分 → 题库可看答案**；首页每日一题；抽题先为错误率加权随机（遗忘曲线先不做）。这些与 P0 是否同一里程碑，提出人未确认。

已知约束（实现时若启动才相关）：LangChain 单链、一张 `document_chunk`、无 OCR、无 LangGraph。本想法若需要「按节点抽题」而不是「按向量搜」，不一定能只靠现有 RAG 链。

---

## 4. 需要以后想清楚的问题

1. `.xmind` 版本与结构是否稳定可解析？解析失败时产品怎么办？
2. 参考答案以导图子树为准，还是允许库里其它文档补充？评分会不会和「只根据资料」的 RAG 原则打架？
3. 十分制有没有校准？同一答案两次打分差多少可接受？
4. 一道题对应多个节点、或导图只有一层时，怎么抽、怎么定位？
5. 是否保存作答历史、错题本？这会不会变成另一套产品？
6. 隐私：作答全文是否出网给 LLM（与现有对话类似）。
7. 板块与知识库一对一还是多库拼一个板块？换库后板块列表怎么变？
8. 标签如何绑到板块：文档级标签够不够，要不要题目级？
9. 随机生成：抽已有问句，还是每次让模型出新题？后者如何保证能定位回原文？
10. 多格式时「定位回材料」对 PDF 页、笔记标题、导图节点是否同一套交互？
11. 「经常出现」按什么加重：连续错、分数低于某档、隔几天未复习？答对后如何降权、间隔如何拉长？
12. 界面要不要画出遗忘曲线，还是只 internally 用曲线决定抽题？

---

## 5. 下一步

提出人要求：**暂不写入 `PRD.md`**。练题/挑战先停在 THINK。**先优化现有 P0**（导入、标签、收藏、检索、RAG 问答）。讨论清楚后再进 PRD / 设计文档。
